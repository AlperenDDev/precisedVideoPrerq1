public class VariableAssignment {
    public void variableAssignmentMethod() {
        string batchName = 'Batch16';
        integer noOfStudents = 50;
        string description = 'This is best batch and we are doing great';
        system.debug('batch name = ' + batchName);
        system.debug('no of studens = ' + noOfStudents);
        system.debug('descprion = ' + description);
    }
}